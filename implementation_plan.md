# PMS 系统自动化 LLM Skill 开发计划

## 💡 产品愿景与核心场景 (User Journey)
**用户期望的交互模式**：
- **日常记录**：AI 作为个人的工作小助手，每天主动询问或接收用户的口语化工作流水（例如：“我今天完成了临分分入转分出的系分编写”）。
- **智能匹配与润色**：当用户下达“报工”指令时，AI 会从系统的任务列表中，自动找到对应的业务任务，并将流水账口语润色为**专业、规范的报工描述**。
- **自动提交**：AI 调用底层的 Skill（MCP 工具）完成真实的系统填报。

本计划旨在为上述场景提供底层的操作能力（Skill），重点实现**自动报工**、**任务查询与拆分**等。

## 第一阶段：API 逆向分析与基础接口联调（当前所处阶段）
**目标**：摸清 PMS 系统的认证机制和关键业务接口，沉淀一套可用的 HTTP 调用代码。
1. **登录与会话保持**：
   - 固化 `Playwright` 自动化登录获取 Cookie 的流程。
   - 实现 Cookie 缓存机制（如存入本地 `auth.json` 或 Redis），避免每次调用都重新拉起浏览器登录。
   - 实现会话过期检测与自动续期（如 401 拦截后触发重新登录）。
2. **核心业务 API 梳理与联调**：
   - [x] **任务查询接口** (`doQueryTaskTreePage2`): 已调通，使用 JSON 参数格式。
   - [x] **获取项目人员接口** (`loadProjectOper`): 能够根据登录账号（如 sunrongnan）动态获取对应的执行人 ID (`operCode`)。
   - [x] **任务拆分/报工接口** (`doDivideTask`): 已获取 Payload。明确了这是报工的第一步核心操作，所需字段如执行人ID可动态获取，部分业务字段（任务类型、事项）给定默认值，优先级由大模型智能判断。
   - [x] **任务更新与撤销接口** (`doUpdateTask`, `doDelTask`): 储备作为容错与状态流转使用。

## 第二阶段：Python API SDK 封装层 (Facade)
**目标**：将散乱的 requests 调用封装成面向对象、健壮的 Python 业务类，屏蔽底层网络逻辑。
1. 创建 `PMSClient` 核心类，内置自动处理 Auth 的 HTTP 客户端。
2. 封装具体的业务方法，例如：
   - `def get_my_tasks(project_no: str) -> list[dict]`
   - `def submit_timesheet(task_id: str, date: str, hours: float, desc: str) -> bool`
   - `def split_task(parent_task_id: str, new_task_name: str, assignee: str) -> str`
3. **增加数据清洗与业务校验逻辑**：
   - 去除接口返回的多余字段，只保留大模型需要知道的核心信息（减少 Token 消耗并提升理解准确率）。
   - **自动过滤超额任务**：基于公式 `剩余可用工时 = planTime - (planTime * taskProgress / 100)` 计算。在查出任务后，底层代码强制过滤，**只将剩余工时 > 8 的任务**作为候选返回给大模型，从根本上杜绝无效匹配或超量报工。

## 第三阶段：大模型 Skill/Tool 定义层 (面向 Claude Code 与龙虾)
**目标**：将封装好的接口按照统一标准暴露给大语言模型，使其能够无缝接入。
1. **采用 MCP (Model Context Protocol) 标准**：
   - 鉴于此 Skill 主要供 Claude Code 和龙虾使用，强烈建议将其封装为一个 **MCP Server**。Claude Code 等支持 MCP 的大模型能够通过标准协议直接连接并调用我们定义的 Tools，无需额外的框架适配。
2. **定义工具与输入 Schema**：
   - 编写 `mcp_server.py`，注册如下 Tool：
     - `QueryTasksTool`: 查询用户的项目和任务列表。
     - `ReportWorkTool`: 提交报工记录。
     - `CreateSubTaskTool`: 拆分并新建子任务。
   - 为每个工具定义严谨的 JSON Schema 和**非常详尽的字段描述**（大模型非常依赖注释来决定如何传递参数）。
3. **错误反馈机制**：
   - 当参数校验失败或接口返回异常时，返回清晰的错误提示文本，让 Claude 能够自我纠错（Self-Reflection）。

## 第四阶段：Agent 组装与编排 (Orchestration)
**目标**：搭建能够接收用户自然语言指令并调度这些 Skill 的智能体。
1. **系统提示词 (System Prompt) 设计**：
   - 设定 Agent 的角色：“你是一个负责公司 PMS 系统的 AI 助理，请遵循以下步骤来协助用户...”。
   - 注入业务先验知识（如：报工前必须先确认当天的任务 ID）。
2. **工作流测试 (Workflow)**：
   - 测试场景 A：“帮我看看我今天在20251102项目里有什么任务，然后帮我在第一个任务上报 8 小时的工，描述写‘接口联调’”。
   - 测试场景 B：“把‘百分百支付信息大屏开发’这个任务拆成前端和后端两个子任务，分别分给张三和李四”。

## 第五阶段：部署与生产化
**目标**：让这个 Skill 能以服务的形式运行，或集成到现有的钉钉/企微机器人中。
1. 剥离本地测试代码，将 Playwright 改为纯粹的 Headless 模式或者容器化运行。
2. 日志监控与审计（防止大模型乱报工）。

---

## 👨‍💻 用户反馈请求
以上是整体的五步开发规划。目前我们在**第一阶段的第 2 步**。
请您过目这份规划，如果大方向没有问题：
**下一步建议**：我们可以立即开始收集**报工提交接口**和**任务拆分接口**的网络请求参数（就像我们刚才测试 `doQueryTaskTreePage2` 一样）。
您看我们接下来是先去抓包“报工接口”，还是“拆分接口”？或者您想优先完善 Cookie 持久化的逻辑？
